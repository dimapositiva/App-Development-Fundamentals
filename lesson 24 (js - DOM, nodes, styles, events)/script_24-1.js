let elements = document.body.children[0];// copy first child in variable
let attribute = elements.getAttribute('data-widget-name'); // get attribute

// show variables in console
console.log(elements);
console.log(attribute);
